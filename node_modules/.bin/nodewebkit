#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nodewebkit/bin/nodewebkit" "$@"
  ret=$?
else 
  node  "$basedir/../nodewebkit/bin/nodewebkit" "$@"
  ret=$?
fi
exit $ret
